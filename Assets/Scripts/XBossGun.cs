using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class XBossGun : Gun
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    private void Reset()
    {
        fireSpeed = 2f;
        gunUseTimer = 2f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
